# Team Meeting Notes — 2026-09-22

**Present:** Person A (Track A - Core AI Pipeline), Person B (Track B - Access & Difficulty), Person C (Track C - Frontend & API), Non-technical Member.

### Agenda
1. Project kickoff, SOP adherence, repository setup.
2. Review core requirements from Section 4.1.
3. Agree on the pipeline-to-API contract (`docs/architecture.md`).

### Discussion Points
- Confirmed that this mini-project strictly targets Core features (Semantic Scholar, Sentence Transformers, Unpaywall, Flesch-Kincaid + Jargon density, React frontend).
- Agreed on JSON data contract: every result will have `relevance_score`, `is_open_access`, `oa_url`, `difficulty` (`level`, `fk_grade`, `jargon_density`), and `why_recommended`.
- Emphasized strict vulnerability disclosures: input sanitization and secure URL rendering.
- Track C will wire up a "Hello World" endpoint so the frontend and backend talk to each other first before complex AI modules are merged.

### Action Items
- **Person A**: Build `metadata.py` and `embeddings.py`.
- **Person B**: Build `open_access.py` and `difficulty.py`.
- **Person C**: Build Flask routes and React skeleton.
- **Non-technical**: Curate 10 benchmark test queries for validation.
