# Team Meeting Notes — 2026-09-25

**Present:** Person A, Person B, Person C, Non-technical Member.

### Agenda
1. Pipeline integration check (~40% milestone).
2. Paywall fallback testing and verification.
3. React UI scannability review against Nielsen Norman Group principles.

### Discussion Points
- Verified that paywall fallback triggers when a user searches for a paywalled paper (e.g. ACM / IEEE papers without immediate OA).
- The fallback feeds the abstract into semantic embedding search, filters candidates by `is_open_access == True`, and marks results with a distinct paywall-alternative badge.
- Reviewed UI styling: Badges for relevance, access, and difficulty must be prominently displayed so users can evaluate results in under 5 seconds.
- Ensured React handles Vercel hosting smoothly with mock demo fallback when the backend URL is not configured.

### Action Items
- Complete end-to-end test runs and record results in `docs/test-log.md`.
- Finalize outline and initial draft in `docs/paper/draft-v1.md`.
